import pow


function call(pow);
    num to_pwr >>9
    num pwr >>4021
    pow(to_pwr,pwr)
}call;
call(math.pow)
Write "$pwrd$"
newln
Write "Total time to run: "
exectime(secs)
newln
Write "Total time to run (ms): "
exectime(ms)
newln
exit()
