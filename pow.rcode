#math.pow
#args: to_pwr, pwr;
#result: to_pwr**pwr
function _pwr(num to_pwr, pwr){
    num init >>to_pwr
    --pow
    for __pwr in range:pow;{
        to_pwr*init
    };
    returns to_pwr;
    

}_pwr;
_pwr(num to_pwr >>to_pwr;;num pwr >>pwr)

